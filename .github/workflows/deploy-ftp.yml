name: 🚀 Smart WooCommerce CI/CD to Hostinger via FTP

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  detect_changes:
    name: 🔍 Detect changed folders
    runs-on: ubuntu-latest
    outputs:
      theme_changed: ${{ steps.detect.outputs.theme_changed }}
    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # cần để diff commit

      - name: 🔎 Detect changes in theme/plugin folders
        id: detect
        run: |
          # Kiểm tra có thay đổi trong theme không
          if git diff --name-only HEAD~1 HEAD | grep -q "wp-content/themes/flatsome-child/"; then
            echo "theme_changed=true" >> $GITHUB_OUTPUT
          else
            echo "theme_changed=false" >> $GITHUB_OUTPUT
          fi

  deploy_theme:
    name: 🎨 Deploy Theme
    needs: detect_changes
    if: needs.detect_changes.outputs.theme_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout source
        uses: actions/checkout@v4

      # (Tùy chọn) build theme nếu cần
      # - name: 🔧 Build theme assets
      #   run: npm install && npm run build

      - name: 📤 Upload WooCommerce Theme via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./wp-content/themes/flatsome-child/
          server-dir: /httpdocs/wp-content/themes/flatsome-child/
          exclude: |
            **/.git*
            **/.github*
            node_modules/**
            .DS_Store
            package-lock.json
